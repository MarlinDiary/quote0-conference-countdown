[project]
name = "quote0-conference-countdown"
version = "0.1.0"
description = "A one-conference deadline countdown for the Quote/0 e-ink display"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "pillow>=11.3,<13",
  "pyyaml>=6.0,<7",
  "requests>=2.32,<3",
]

[dependency-groups]
dev = []

[tool.uv]
package = false
